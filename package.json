{
  "name": "hitokoto-server",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"cd backend && go run ./cmd/server\" \"cd frontend && pnpm dev\"",
    "build": "cd backend && go build -o ../dist/server ./cmd/server && cd ../frontend && pnpm build",
    "build:backend": "cd backend && go build -o ../dist/server ./cmd/server",
    "build:frontend": "cd frontend && pnpm build",
    "install:all": "cd frontend && pnpm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
